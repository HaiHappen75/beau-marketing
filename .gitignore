# dependencies
/node_modules
.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# typescript incremental build cache
tsconfig.tsbuildinfo

# production
/build
/dist

# payload — uploaded media (local dev). Persist via volume/object storage in prod.
/media

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (never commit secrets)
.env
.env*.local

# claude code local settings (personal)
.claude/settings.local.json

# editor / os
.idea
.vscode/*
!.vscode/extensions.json
